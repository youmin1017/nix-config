local smw = require("plugins.split-monitor-workspaces")
smw.setup({
  keep_focused = true,
})

-- hl.config({
--   plugin = {
--     split_monitor_workspaces = {
--       keep_focused = true,
--     },
--   },
-- })
